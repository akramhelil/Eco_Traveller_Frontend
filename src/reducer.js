
const initialState = {
  currentTraveller: null,
  currentTrip: null,
  posts: [],
  trips: []
}

function reducer (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
